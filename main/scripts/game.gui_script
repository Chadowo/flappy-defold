function on_message(self, message_id, message, sender)
	if message_id == hash('update_score') then 
		-- Update the score on screen
		local score_node = gui.get_node('score')
		gui.set_text(score_node, message.score)
	end
end